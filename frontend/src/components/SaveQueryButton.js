import styles from '@/styles/components/SaveQueryButton.module.css'

const SaveQueryButton = () => {
  return (
    <button className={styles.saveQuery}>Save Query</button>
  )
}

export default SaveQueryButton;
